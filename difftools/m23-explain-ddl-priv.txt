# Matrix 23: EXPLAIN shapes, DDL options, privilege errors, misc semantics
>>>
CREATE TABLE x1(a INT PRIMARY KEY, b INT, c VARCHAR(20), KEY kb(b));
INSERT INTO x1 VALUES (1,1,'aa'),(2,2,'bb'),(3,3,'cc');
EXPLAIN SELECT * FROM x1 WHERE a = 1;
EXPLAIN SELECT * FROM x1 WHERE b > 1;
EXPLAIN FORMAT = 'brief' SELECT * FROM x1 WHERE a IN (1,2);
EXPLAIN FORMAT = 'dot' SELECT * FROM x1 WHERE a = 1;
EXPLAIN FORMAT = 'hint' SELECT * FROM x1 WHERE a = 1;
EXPLAIN VERBOSE SELECT * FROM x1 WHERE a = 1;
EXPLAIN ANALYZE SELECT * FROM x1;
EXPLAIN SELECT * FROM x1 WHERE c = 'aa' AND b = 1 ORDER BY b LIMIT 1;
ALTER TABLE x1 ADD COLUMN d INT, ALGORITHM=INPLACE;
ALTER TABLE x1 ADD COLUMN e INT, ALGORITHM=COPY;
ALTER TABLE x1 ADD COLUMN f INT, LOCK=NONE;
ALTER TABLE x1 DROP COLUMN d, ALGORITHM=INSTANT;
CREATE INDEX kb2 ON x1(c(10));
CREATE INDEX kb3 ON x1(b) ALGORITHM=INPLACE LOCK=NONE;
CREATE TABLE x2(a INT) ENGINE=InnoDB AUTO_INCREMENT=100;
INSERT INTO x2 VALUES (DEFAULT);
SELECT * FROM x2;
CREATE USER 'u1'@'localhost';
CREATE USER 'u1'@'localhost';
GRANT SELECT ON dtq.h1 TO 'u1'@'localhost';
GRANT BOGUS_PRIV ON *.* TO 'u1'@'localhost';
REVOKE INSERT ON *.* FROM 'nobody'@'nowhere';
DROP USER 'u1'@'localhost';
DROP USER 'u1'@'localhost';
SET PASSWORD FOR 'ghost'@'localhost' = 'x';
RENAME USER 'ghost'@'localhost' TO 'g2'@'localhost';
UPDATE x1, x1 AS x2 SET x1.b = x2.b WHERE x1.a = x2.a;
DELETE x1 FROM x1 WHERE a = 1;
SELECT _latin1'abc', CONVERT('abc' USING gbk);
SELECT _gbk'abc';
SELECT b'0101' + 0, 0b0101 + 1, x'41' + 0;
SELECT 0x + 1;
SELECT 1_000, .5, 5., 1e, 007;
LOCK TABLES x1 READ;
UNLOCK TABLES;
CHECK TABLE x1;
CHECKSUM TABLE x1;
OPTIMIZE TABLE x1;
ANALYZE TABLE x1;
FLUSH TABLES;
FLUSH STATUS;
