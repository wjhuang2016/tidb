# Matrix 21: SET round-trip, PREPARE, SAVEPOINT edges
>>>
SET SESSION sql_mode = 'STRICT_TRANS_TABLES,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,ANSI_QUOTES';
SELECT @@sql_mode;
SET sql_mode = 'bogus_mode';
SET sql_mode = CONCAT('STRICT', '_TRANS_TABLES');
SELECT @@sql_mode;
SET NAMES utf8mb4 COLLATE bogus_collation;
SET NAMES bogus_charset;
SET NAMES utf8mb4;
SET character_set_results = bogus;
SET character_set_results = NULL;
SELECT @@character_set_results;
SET SESSION max_execution_time = 'abc';
SET SESSION tidb_distsql_scan_concurrency = -1;
SET SESSION autocommit = 2;
SET @usr = 5, @usr2 = @usr * 2;
SELECT @usr, @usr2, @undefined_var;
SET GLOBAL max_connections = DEFAULT;
SELECT @@max_connections;
PREPARE st FROM 'SELECT ? + 1';
SET @p = 41;
EXECUTE st USING @p;
DEALLOCATE PREPARE st;
EXECUTE st USING @p;
PREPARE st2 FROM 'SELECT no_such_col';
PREPARE st3 FROM 'CREATE TABLE pp(a INT)';
EXECUTE st3;
DROP TABLE pp;
BEGIN;
SAVEPOINT sp1;
SAVEPOINT sp1;
ROLLBACK TO no_such_sp;
RELEASE SAVEPOINT no_such_sp;
SAVEPOINT sp2;
ROLLBACK TO sp1;
COMMIT;
SELECT `weird``col` FROM DUAL;
SELECT @@invalid_system_variable;
SELECT @@SESSION.sql_mode, @@GLOBAL.sql_mode;
FLUSH PRIVILEGES;
SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;
SELECT @@transaction_isolation;
SET SESSION TRANSACTION ISOLATION LEVEL SERIALIZABLE;
